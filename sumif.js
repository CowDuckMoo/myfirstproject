// Create a function called sumIfPossible. :D 
// This function should take an array as input, and sum the values together if possible. :D
// If the variables are not the correct data type, skip over it, but keep track of how many skips have occurred